### Font
#
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: SF Pro Display 12

### OTHER
#
# xwayland disable
#
set $mod Mod4
set $alt Mod1
set $altr Mod5


### Wallpaper
#
# Output configuraion
# Default wallpaper (more /usr/share/backgrounds/sway/)

include "$HOME/.cache/wal/colors-sway"

exec --no-startup-id wal -R # -i $wallpaper
output * bg $wallpaper fill

set $wallpapers $HOME/Pictures/Wallpapers/Road
bindsym $mod+w exec "wal -i $wallpapers -o wal-set"

### Clamshell Mode
#
# AKA Closed Display mode. This mode is where your laptop lid is closed and you have external monitor(s) as outputs. As of Sway 1.1 you can use switch events to drive certain functionality, such as disabling an output upon a laptop lid being closed. Pass your laptop output name into the following, which can by found with: swaymsg -t get_outputs for example eDP-1.
# 
# set $laptop <laptop_output_identifier>
# bindswitch --reload --locked lid:on output $laptop disable
# bindswitch --reload --locked lid:off output $laptop enable
 
### HiDPI
#
# HiDPI can be enabled via output and its scale option.
# 
# output <name> scale <I>
# 
# <I> is the integer scaling factor (usually 2 for HiDPI screens)
# 
# If scaling is active, it has to be considered when defining relative positions. For example, if the scaling factor for the left monitor (HDMI1) is 2, the relative position for the right output has to be divided by 2. This example is illustrated by the configuration below:
# 
# output HDMI1 scale 2
# output HDMI1 pos 0 0 res 3200x1800
# output eDP1 pos 1600 0 res 1920x1080
# Note that the x-pos of eDP1 is 1600 = 3200/2.
 
# After unplugging an external display, some applications appear too large on my HiDPI screen
# See wlroots issue #1119. This is an Xwayland bug fixed in 1.20.9.
# exec_always --no-startup-id xrdb -merge ~/.Xresources
exec --no-startup-id xrdb -load ~/.Xresources

### window colors
#
#                       border      background      text	    indicator
client.focused		$color0	    $background	    $foreground	    $color7	$background
client.unfocused	$color0	    $background	    $foreground	    $color7	$background
client.focused_inactive $color0	    $background	    $foreground	    $color7	$background
client.urgent		$color0	    $background	    $foreground	    $color7	$background
#
# set $opacity 0.95
# for_window [class=".*"] no_focus opacity $opacity
# for_window [app_id=".*"] no_focus opacity $opacity


### Monitors
#
# You can get the names of your outputs by runnign: swaymsg -t get_outputs
#
# Known monitors 
#
set $tv_samsung_zamenhofa 'Samsung Electric Company SAMSUNG 0x00000001'
set $tv_hanspree_krosno 'Unknown HSG1116 0x00000101'
set $tv_samsung_lakowa 'Samsung Electric Company SAMSUNG 0x00000000'
set $monitor_asus 'Ancor Communications Inc ASUS VX279 F6LMTJ001494'
set $monitor_acer 'Acer Technologies V223HQ LES0C0024000'
set $monitor_dell_moj 'Dell Inc. DELL U2414H 9TG463AS6R4L'
set $monitor_dell_sii_lewy 'Dell Inc. DELL P2419H G4YBPM2'
set $monitor_dell_sii_prawy 'Dell Inc. DELL P2419H 16HR3Q2'

set $laptop_screen 'Chimei Innolux Corporation 0x15B1 0x00000000'
set $primary $monitor_dell_sii_lewy
# set $primary $monitor_dell_moj
set $secondary $monitor_dell_sii_prawy

output $laptop_screen resolution 1920x1080 position 0 0
output $primary resolution 1920x1080 position 1920 0 transform 270
# output $primary resolution 1920x1080 position 1920 0 
output $secondary resolution 1920x1080 position 3000 0 transform 90

### Idle configuration
#
# Example
#
# swayidle -w \
#     timeout 300 'swaylock -f -c 00000' \
#     timeout 600 'swaymsg "output * dpms off"' \
# 	resume 'swaymsg "output * dpms on"' \
#     before-sleep 'swaylock -f -c 00000'
#
#This will lock yout screen after 300 seconds of inactiviti, then turn off
#your displays after another 300 seconds, and turn your screens back when resumed. 
#It will also #lock your screen before your computer goes to sleep.


### Input configuration
#
# Example 
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#	dwt enabled
#	tap enabled
#	natural_scroll enabled
#	middle_emulation enabled
#   }
#
input "AT Translated Set 2 keyboard" xkb_model "precision_m"
input "Dell WMI hotkeys" xkb_model "precision_m"
input * xkb_layout "pl"
input * xkb_numlock enable
# input * xkb_variant "colemak,,typewriter"
# input * xkb_options "grp:win_space_toggle"
# input "AT Translated Set 2 keyboard" xkb_options lv3:ralt_alt
# input "Dell WMI hotkeys" xkb_options lv3:ralt_alt


# kill focused window
bindsym $mod+c kill

# thin borders
hide_edge_borders both

# use Mouse+Mod1 to drag floating windows to their wanted position
#floating_modifier Mod1
floating_modifier $mod normal

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# sticky window
bindsym $mod+Shift+s sticky toggle

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
# bindsym $altr+space focus mode_toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
# bindsym Mod1+d focus child

### i3-gaps settings
#
# You can also use any non-zero value if you'd like to have a border
for_window [class="^.*"] border pixel 0
smart_borders on

gaps inner 45
gaps outer 0

default_border pixel
default_floating_border normal
hide_edge_borders none
popup_during_fullscreen smart

focus_on_window_activation smart

bindsym $mod+equal gaps inner all plus 5
bindsym $mod+minus gaps inner all minus 5

bindsym $mod+o border pixel 2
bindsym $mod+Shift+o border none
 
# Only enable outer gaps when there is exactly one container
# smart_gaps inverse_outer

# Activate smart borders (only when there are effectively no gaps)
# smart_borders no_gaps


######################################################################################
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

### Terminal
#
set $term alacritty
bindsym $mod+Return exec $term

#####################################################################################
################################## EXIT
# 
# #set $mode_system System (l) lock, (e) logout, (s) suspend, (r) reboot, (Shift+s) shutdown
# set $mode_system System (l) logout, (r) reboot, (s) shutdown
# mode "$mode_system" {
#     bindsym l exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/swayexit.sh logout, mode "default"
# #    bindsym e exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/swayexit.sh lock, mode "default"
# #    bindsym s exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/swayexit.sh suspend, mode "default"
#     bindsym r exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/swayexit.sh reboot, mode "default"
#     bindsym s exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/swayexit.sh shutdown, mode "default"
# 
#     # back to normal: Enter or Escape
#     bindsym Return mode "default"
#     bindsym Escape mode "default"
# }
# 

### XF86 CONTROLS
#
bindsym XF86PowerOff mode "$mode_system"
# bindsym XF86AudioRaiseVolume exec "amixer sset Master '2%+'"
# bindsym XF87AudioLowerVolume exec "amixer sset Master '2%-'"
# bindsym XF86AudioMute exec "amixer sset Master toggle"
bindsym XF86AudioRaiseVolume exec "pactl set-sink-volume @DEFAULT_SINK@ +4%"
bindsym XF86AudioLowerVolume exec "pactl set-sink-volume @DEFAULT_SINK@ -4%"
bindsym XF86AudioMute exec "pactl set-sink-mute @DEFAULT_SINK@ toggle"
bindsym XF86TouchpadToggle exec "pactl set-source-mute @DEFAULT_SOURCE@ toggle"
# bindsym XF86AudioMicMute exec "pactl set-source-mute @DEFAULT_SOURCE@ toggle"

exec pactl set-source-mute @DEFAULT_SOURCE@ 0

bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# bindsym XF86Sleep exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/swayexit.sh suspend

# Screen brightness controls
bindsym XF86MonBrightnessUp exec brightnessctl s +5% # increase screen brightness
bindsym XF86MonBrightnessDown exec brightnessctl s 5%- # decrease screen brightness

# bindsym Scroll_Lock exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/i3lock.sh

##################################################################################
#################### WORKSPACES
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.

### MAIN WORKSPACES
#
# $ws0 jest do zapisywania screenshotow jako notatek
# set $ws00 "00:!: "
#     assign [class="flameshot"] $ws00
#     for_window[class="flameshot"] floating enable

for_window [class="org.qt-project.qtcreator"] floating enable 

set $ws11 "11:1: "
set $ws21 "21:2: "
set $ws31 "31:3: "
set $ws41 "41:4: "
set $ws51 "51:5: "
set $ws61 "61:6: "
set $ws71 "71:7: "
    assign [class="Caprine"] $ws71
    #assign [class="Skype"] $ws71
    assign [class="Cryptocat"] $ws71
    for_window [class="Cryptocat"] floating enable 
    assign [class="qTox"] $ws71
    for_window [class="qTox"] floating enable
set $ws81 "81:8: "	
    assign [class="Thunderbird"] $ws81
set $ws91 "91:9: "
    assign [class="Audacious"] $ws91 
    assign [class="Anoise"] $ws91
    # for_window[class="Anoise"] floating enable
set $ws101 "101:10: "

workspace $ws11 output $laptop_screen
workspace $ws21 output $laptop_screen
workspace $ws31 output $laptop_screen
workspace $ws41 output $laptop_screen
workspace $ws51 output $laptop_screen
workspace $ws61 output $laptop_screen
workspace $ws71 output $laptop_screen
workspace $ws81 output $laptop_screen
workspace $ws91 output $laptop_screen
workspace $ws101 output $laptop_screen

# WWW
bindsym $mod+1 workspace $ws11
# C++
bindsym $mod+2 workspace $ws21
# Python
bindsym $mod+3 workspace $ws31
# Bash
bindsym $mod+4 workspace $ws41
# i3
bindsym $mod+5 workspace $ws51
# Job / Physics
bindsym $mod+6 workspace $ws61
# Messenger / Other
bindsym $mod+7 workspace $ws71
# Mail / RSS
bindsym $mod+8 workspace $ws81
# Music / Other
bindsym $mod+9 workspace $ws91
# Movie / Downloads
bindsym $mod+0 workspace $ws101

bindsym $mod+Shift+1 move container to workspace $ws11
bindsym $mod+Shift+2 move container to workspace $ws21
bindsym $mod+Shift+3 move container to workspace $ws31
bindsym $mod+Shift+4 move container to workspace $ws41
bindsym $mod+Shift+5 move container to workspace $ws51
bindsym $mod+Shift+6 move container to workspace $ws61
bindsym $mod+Shift+7 move container to workspace $ws71
bindsym $mod+Shift+8 move container to workspace $ws81
bindsym $mod+Shift+9 move container to workspace $ws91
bindsym $mod+Shift+0 move container to workspace $ws101

### Aditional Workspaces
#
# www
set $ws12 "12:"

# C++
set $ws22 "22:"
#    assign [class="Code"] $ws22
# Python
set $ws32 "32:"

# Bash
set $ws42 "42:"

#
set $ws52 "52:"

# Job / Physics
set $ws62 "62:"

# Messenger / Other
set $ws72 "72:"
    assign [class="Pidgin"] $ws72
    for_window [class="Pidgin"] floating window
# Mail / RSS
set $ws82 "82:"
    assign [class="Liferea"] $ws82

# Music / Other
set $ws92 "92:"

# Movies / Downloads
set $ws102 "102:"
    assign [class="Deluge"] $ws102
    assign [class="Nicotine"] $ws102

workspace $ws12 output $laptop_screen
workspace $ws22 output $laptop_screen 
workspace $ws32 output $laptop_screen
workspace $ws42 output $laptop_screen 
workspace $ws52 output $laptop_screen 
workspace $ws62 output $laptop_screen
workspace $ws72 output $laptop_screen
workspace $ws82 output $laptop_screen
workspace $ws92 output $laptop_screen
workspace $ws102 output $laptop_screen

bindsym $alt+1 workspace $ws12 
bindsym $alt+2 workspace $ws22
bindsym $alt+3 workspace $ws32
bindsym $alt+4 workspace $ws42
bindsym $alt+5 workspace $ws52
bindsym $alt+6 workspace $ws62
bindsym $alt+7 workspace $ws72
bindsym $alt+8 workspace $ws82
bindsym $alt+9 workspace $ws92
bindsym $alt+0 workspace $ws102

bindsym $alt+Shift+1 move container to workspace $ws12
bindsym $alt+Shift+2 move container to workspace $ws22
bindsym $alt+Shift+3 move container to workspace $ws32
bindsym $alt+Shift+4 move container to workspace $ws42
bindsym $alt+Shift+5 move container to workspace $ws52
bindsym $alt+Shift+6 move container to workspace $ws62
bindsym $alt+Shift+7 move container to workspace $ws72
bindsym $alt+Shift+8 move container to workspace $ws82
bindsym $alt+Shift+9 move container to workspace $ws92
bindsym $alt+Shift+0 move container to workspace $ws102

### Primary workspaces
#
set $ws01primary "1:1: " 
set $ws02primary "2:2: " 
set $ws03primary "3:3: " 
set $ws04primary "4:4: " 
set $ws05primary "5:5: " 
set $ws06primary "6:6: " 
set $ws07primary "7:7: " 
set $ws08primary "8:8: " 
set $ws09primary "9:9: " 
set $ws10primary "10:10: " 

workspace $ws01primary output $primary
workspace $ws02primary output $primary
workspace $ws03primary output $primary
workspace $ws04primary output $primary
workspace $ws05primary output $primary
workspace $ws06primary output $primary
workspace $ws07primary output $primary
workspace $ws08primary output $primary
workspace $ws09primary output $primary
workspace $ws10primary output $primary

bindsym Ctrl+1 workspace $ws01primary 
bindsym Ctrl+2 workspace $ws02primary 
bindsym Ctrl+3 workspace $ws03primary
bindsym Ctrl+4 workspace $ws04primary
bindsym Ctrl+5 workspace $ws05primary
bindsym Ctrl+6 workspace $ws06primary
bindsym Ctrl+7 workspace $ws07primary
bindsym Ctrl+8 workspace $ws08primary
bindsym Ctrl+9 workspace $ws09primary
bindsym Ctrl+0 workspace $ws10primary

bindsym Ctrl+Shift+1 move container to workspace $ws01primary
bindsym Ctrl+Shift+2 move container to workspace $ws02primary
bindsym Ctrl+Shift+3 move container to workspace $ws03primary
bindsym Ctrl+Shift+4 move container to workspace $ws04primary
bindsym Ctrl+Shift+5 move container to workspace $ws05primary
bindsym Ctrl+Shift+6 move container to workspace $ws06primary
bindsym Ctrl+Shift+7 move container to workspace $ws07primary
bindsym Ctrl+Shift+8 move container to workspace $ws08primary
bindsym Ctrl+Shift+9 move container to workspace $ws09primary
bindsym Ctrl+Shift+0 move container to workspace $ws10primary

### Secondary workspaces
#
set $ws01secondary "1: 1: " 
set $ws02secondary "2: 2: " 
set $ws03secondary "3: 3: " 
set $ws04secondary "4: 4: " 
set $ws05secondary "5: 5: " 
set $ws06secondary "6: 6: " 
set $ws07secondary "7: 7: " 
set $ws08secondary "8: 8: " 
set $ws09secondary "9: 9: " 
set $ws10secondary "10: 10: " 
#
# set $ws01secondary "1:1: " 
# set $ws02secondary "2:2: " 
# set $ws03secondary "3:3: " 
# set $ws04secondary "4:4: " 
# set $ws05secondary "5:5: " 
# set $ws06secondary "6:6: " 
# set $ws07secondary "7:7: " 
# set $ws08secondary "8:8: " 
# set $ws09secondary "9:9: " 
# set $ws10secondary "10:10: " 

workspace $ws01secondary output $secondary 
workspace $ws02secondary output $secondary
workspace $ws03secondary output $secondary
workspace $ws04secondary output $secondary
workspace $ws05secondary output $secondary
workspace $ws06secondary output $secondary
workspace $ws07secondary output $secondary
workspace $ws08secondary output $secondary
workspace $ws09secondary output $secondary
workspace $ws10secondary output $secondary

bindsym $altr+1 workspace $ws01secondary  
bindsym $altr+2 workspace $ws02secondary  
bindsym $altr+3 workspace $ws03secondary  
bindsym $altr+4 workspace $ws04secondary  
bindsym $altr+5 workspace $ws05secondary  
bindsym $altr+6 workspace $ws06secondary  
bindsym $altr+7 workspace $ws07secondary  
bindsym $altr+8 workspace $ws08secondary  
bindsym $altr+9 workspace $ws09secondary  
bindsym $altr+0 workspace $ws10secondary  

bindsym $altr+Shift+1 move container to workspace $ws01secondary
bindsym $altr+Shift+2 move container to workspace $ws02secondary
bindsym $altr+Shift+3 move container to workspace $ws03secondary
bindsym $altr+Shift+4 move container to workspace $ws04secondary
bindsym $altr+Shift+5 move container to workspace $ws05secondary
bindsym $altr+Shift+6 move container to workspace $ws06secondary
bindsym $altr+Shift+7 move container to workspace $ws07secondary
bindsym $altr+Shift+8 move container to workspace $ws08secondary
bindsym $altr+Shift+9 move container to workspace $ws09secondary
bindsym $altr+Shift+0 move container to workspace $ws10secondary

# switching between workspace
# http://i3wm.org/docs/userguide.html#_changing_named_workspaces_moving_to_workspaces
# bindsym $mod+p workspace next
# bindsym $mod+n workspace prev

bindsym $mod+Ctrl+p move container to workspace next
bindsym $mod+Ctrl+n move container to workspace prev

bindsym $mod+Ctrl+greater move workspace to output right
bindsym $mod+Ctrl+less move workspace to output left
# bindsym $mod+d workspace back_and_forth


#######################################################################################
########################## BAR
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	strip_workspace_numbers yes
#       exec ~/.config/i3/i3status.sh
        status_command i3status -c /home/fm/.config/sway/i3status.conf
#       status_command i3blocks -c ~/.config/i3/i3blocks.conf
        workspace_buttons yes
        position top
	font pango: Roboto, FontAwesome5Free, FontAwesome5Brands

    colors {
	background $background
	separator $color0
#			    borders		background	    text
	focused_workspace   $background		$color2		    $foreground
	inactive_workspace  $background		$background	    $color1
#	urgent_workspace    $urgent-bg-color	$urgent-bg-color    $text-color
    }
}

#########################################################################################
##################### DMENU 

# Your preferred application launcher
# Note: it's recommended that you pass the final command to sway
set $menu dmenu_path | dmenu | xargs swaymsg exec --

# j4-dmenu-desktop -- A faster replacement for i3-dmenu-desktop
# https://github.com/enkore/j4-dmenu-desktop
# https://aur.archlinux.org/packages/j4-dmenu-desktop-git/

# bindsym $mod+d exec rofi -show drun -theme android_notification
# bindsym $mod+d exec rofi -show run -lines 8 -eh 2 -width 100 -padding 800 -opacity "85" -bw 0 -bc $bg-color -bg $bg-color -fg $text-color -hlbg $bg-color -hlfg "#9575cd" -font "System San Francisco Display 18"

# bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --dmenu="dmenu -i -nb "$foreground" -nf "$background" -sb "$background" -sf "$foreground" -fn 'SF mono:light:pixelsize=16'" # -l 5"
# bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --dmenu="dmenu -i -fn 'SF mono:light:pixelsize=16'" # -l 5"

# bindsym $mod+d exec --no-startup-id dmenu_run -nb "$foreground" -nf "$background" -sb "$background" -sf "$foreground"" # -l 5"

bindsym $mod+d exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/application_wrappers/dmenu.sh
bindsym $mod+p exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/application_wrappers/passmenu.sh

#######################################################################################
################################# RESIZE

# resize window (you can also use the mouse for that)
mode "resize" {
        # back to normal: Enter or Escape or Mod1+r
        #bindsym Return mode "default"
        #bindsym Escape mode "default"
        #bindsym $mod+r mode "default"
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

##################################################################################
#################### SCRATCHPAD
# move the currently focused window to the scratchpad
bindsym $mod+Shift+grave move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows,y this command cycles through them.
bindsym $mod+grave scratchpad show

#########################################################################################
################### AUTOSTART
# seat <name> hide_cursor <timeout>

# Unclutter
# it appears that unclutter-xfixes does not work on wayland
exec --no-startup-id unclutter --timeout 2 --jitter 2
exec --no-startup-id pidgin
exec --no-startup-id aplay $HOME/.sounds/Windows98/Windows_98/Windows_98_startup.wav

# Transparent windows
exec_always --no-startup-id python3 ${ENVIRONMENT_SCRIPTS}/inactive-windows-transparency.py

# Redshift / gammastep
# gammastep is better redshift
# exec_always --no-startup-id ${ENVIRONMENT_SCRIPTS}/application_wrappers/redshift.sh
exec --no-startup-id gammastep

# Firefox black theme?
exec_always ${ENVIRONMENT_SCRIPTS}/import-gsettings/import-gsettings.sh \
    gtk-theme:gtk-theme-name \
    icon-theme:gtk-icon-theme-name \
    cursor-theme:gtk-cursor-theme-name

bindsym Print exec --no-startup-id grim "$HOME/Pictures/Screenshots/$(date '+%Y%M%d_%H%m%S').png"
bindsym Shift+Print exec --no-startup-id grim -g "$(slurp)" "$HOME/Pictures/Screenshots/$(date '+%Y%M%d_%H%m%S').png"

# bindsym Print exec --no-startup-id ${ENVIRONMENT_SCRIPTS}/screenshot_flameshot.sh full
# bindsym Shift+Print exec --no-startup-id $HOME/.scirpts/screenshot_flameshot.sh gui

# bindsym $mod+u exec ${ENVIRONMENT_SCRIPTS}/translate.sh
